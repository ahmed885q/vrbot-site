{
  "name": "vrbot",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm --workspace @vrbot/web run dev",
    "build": "npm --workspace @vrbot/web run build",
    "lint": "npm --workspace @vrbot/web run lint"
  }
}